main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ ]
depend: [ ItemsAdder, Vault ]
loadbefore: [ ]
#libraries: [ "${libraries}" ]
commands:
  iahookdisable:
  iahookreload:
