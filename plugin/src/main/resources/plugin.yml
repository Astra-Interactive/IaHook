main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ ]
depend: [ ]
loadbefore: [ ]
#libraries: [ "${libraries}" ]
commands:
  add:
  rickandmorty:
  atempreload:
    description: Reload plugin
    permission: iahook.reload
    permission-message: You do not have permission!
    usage: /<command>
  adamage:
    description: Damage player
    permission: iahook.damage
    permission-message: You do not have permission!
    usage: /<command> <player> amount
  atempgui:
    description: Shows template gui
    usage: /<command>
  translation:
